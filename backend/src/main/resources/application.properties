spring.application.name=backend

# ===========================================
# 远程数据源配置（主数据源）
# ===========================================
spring.datasource.remote.url=jdbc:postgresql://118.178.121.69:65432/secp?currentSchema=tsdb
spring.datasource.remote.username=wulian_iot_readonly
spring.datasource.remote.password=qSDyK88fQNv4GEpc
spring.datasource.remote.driver-class-name=org.postgresql.Driver

# ===========================================
# 本地数据源配置（记录统计结果）
# ===========================================
spring.datasource.local.url=jdbc:postgresql://localhost:5432/backend_stats
spring.datasource.local.username=postgres
spring.datasource.local.password=root
spring.datasource.local.driver-class-name=org.postgresql.Driver

# ===========================================
# JPA配置（细节移至DataSourceConfig中按数据源分别设置）
# ===========================================
spring.jpa.show-sql=true

# ===========================================
# WebSocket/服务器配置
# ===========================================
server.port=8080

# ===========================================
# 日志配置
# ===========================================
logging.level.org.springframework.web=INFO
logging.level.com.example.backend=DEBUG
